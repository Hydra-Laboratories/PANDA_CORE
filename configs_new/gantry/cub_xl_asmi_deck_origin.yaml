# Candidate ASMI gantry configuration for the deck-origin refactor.
#
# This is NOT a drop-in replacement for configs/gantry/cub_xl_asmi.yaml until
# staged hardware validation passes. It encodes the CubOS deck frame:
# front-left-bottom origin, +X right, +Y back, +Z up, -Z down.
#
# Estimated work volume from the CubOS-XL / Genmitsu PROVerXL 4030 V2 setup.
# Confirm live $130/$131/$132 and jog directions before using on hardware.

serial_port: /dev/ttyUSB0

cnc:
  homing_strategy: standard
  total_z_height: 100.0
  y_axis_motion: head
  # Absolute deck-frame Z clearance plane for home/park/edge-risk moves.
  structure_clearance_z: 85.0

working_volume:
  x_min: 0.0
  x_max: 400.0
  y_min: 0.0
  y_max: 300.0
  z_min: 0.0
  z_max: 100.0
