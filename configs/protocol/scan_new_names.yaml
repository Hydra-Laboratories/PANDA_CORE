# Generic scan protocol using deck-origin scan naming.
# Scan heights are absolute deck-frame Z planes, not labware-relative offsets.

protocol:
  - move:
      instrument: uvvis
      position: plate_1.A1

  - scan:
      plate: plate_1
      instrument: uvvis
      method: measure
      measurement_height: 10.0
