# Progress — 2026-04-13

## Protocol validation for `mofcat_deck.yaml`

### What was done

- Ran `python setup/validate_setup.py` with MOFcat-related configs to verify the deck loads and full setup (gantry + deck + board + protocol) passes bounds checks.

### Results

1. **Deck-only (`configs/deck/mofcat_deck.yaml`)**  
   Loads successfully: one well plate (`plate_1`, 96 wells), schema OK.

2. **Full validation with `configs/protocol/protocol.sample.yaml`**  
   **Failed** — not due to the deck. Pydantic rejected the protocol: `dispense` is not a registered protocol command (AGENTS.md notes `dispense` is internal; YAML should use `transfer`).

3. **Full validation with `configs/protocol/move.yaml`**  
   **PASS** — with:
   - `configs/gantry/cubos_xl.yaml`
   - `configs/deck/mofcat_deck.yaml`
   - `configs/board/mofcat_board.yaml`
   - `configs/protocol/move.yaml`  
   Deck positions and gantry positions (96 wells × 1 instrument) are within working volume.

### Notes

- Validation is offline (mock gantry / mock board path); no hardware was run.
