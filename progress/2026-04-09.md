# 2026-04-09 — Holder Labware Scaffolding

## Summary

Added first-pass deck labware support for four physical holder fixtures:
`TipHolder`, `TipDisposal`, `WellPlateHolder`, and `VialHolder`.

## What Was Done

- Added `src/deck/labware/holder.py` with:
  - `HolderLabware` base class for bounding-box fixtures
  - `LabwareSlot` metadata model for future nested-labware placement
  - concrete holder classes with fixed bounding-box dimensions:
    - `TipHolder` = `138 x 66 x 22` mm
    - `TipDisposal` = `198 x 62 x 30` mm
    - `WellPlateHolder` = `100.0 x 155.0 x 14.8` mm
    - `VialHolder` = `36.2 x 300.2 x 35.1` mm with default `slot_count = 9`
- Extended `Labware` with `iter_positions()` so generic validation logic can reason about all addressable points without hard-coding concrete labware types.
- Updated `WellPlate` and `Vial` to implement `iter_positions()`.
- Refactored deck bounds validation to use the new polymorphic position API instead of branching on `WellPlate` vs `Vial`.
- Extended deck YAML support with new discriminated entry types:
  - `tip_holder`
  - `tip_disposal`
  - `well_plate_holder`
  - `vial_holder`
- Added holder-slot YAML support so holders can expose named sub-locations for future nested labware placement.
- Added tests covering:
  - fixed holder dimensions
  - deck resolution for holder anchors and slots
  - YAML loading for holder fixtures
  - bounds validation for holder slots
- Updated `README.md` and `AGENTS.md` to document the new labware types.

## Design Decisions

- Holder `location` is currently treated as a generic deck-space reference point / anchor.
- Bounding-box dimensions are stored now for future collision modeling, but collision math/orientation is not implemented in this task.
- Holder `slots` are metadata-only for now: they provide addressable coordinates and intended compatible labware types, but do not yet instantiate or contain nested `Vial`/`WellPlate` objects automatically.
- Holder slot Z defaults to the holder's resolved Z when omitted in YAML.

## Verification

- `pytest tests/test_holder_labware.py` -> passed
- `pytest tests/test_labware.py tests/test_deck.py tests/test_deck_loader.py tests/validation/test_bounds_validation.py tests/setup/test_protocol_setup.py` -> passed

## Open Questions

- What physical reference should each holder `location` represent long-term: center, front-left corner, A1-equivalent, or another fixture datum?
- For `WellPlateHolder`, should the holder expose a single canonical slot name like `plate`, or should it eventually resolve directly into the contained well plate geometry?
- For `VialHolder`, what are the exact nine vial slot coordinates and naming convention (`vial_1`..`vial_9`, row/column labels, etc.)?
- Should holders eventually own/contain nested labware instances, or should deck YAML keep holders and contained labware as separate top-level objects linked by slot names?

## Design Follow-Up

- Revisited the current A1/orientation approach:
  - The existing `WellPlate` two-point calibration is good for manually measured placement because it captures both anchor and orientation from real deck points.
  - It is less ideal as the universal representation for all labware, especially holders, because fixtures do not naturally have an `A1` semantic and the current generic `location` field is ambiguous.
- Recommended next-step design direction:
  - Separate `anchor` semantics from `orientation`.
  - Use explicit anchor metadata such as `anchor_type` (`a1_center`, `slot_1_center`, `center`, `drop_center`, etc.).
  - Use a simple UI/YAML-friendly orientation field such as `rotation_deg: 0|90|180|270` for CAD-defined fixtures and generated layouts.
  - Keep the existing two-point calibration mode for physically measured well plates.
