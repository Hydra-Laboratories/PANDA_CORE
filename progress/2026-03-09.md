# Progress — 2026-03-09

## WPos Enforcement in the Mill Driver

### Work Done
- Enforced WPos mode at the mill driver level by setting `$10=0` and `G90`
  on connect.
- Added WCO caching so `Mill.current_coordinates()` can convert reported MPos
  into WPos when needed.
- Added `Mill.machine_coordinates()` for raw MPos inspection.
- Updated the mock serial flow to emit WPos-compatible status strings.
- Fixed Python 3.9 typing compatibility around optional total Z height.
- Added unit coverage around WPos reporting, MPos-to-WPos conversion,
  `machine_coordinates()`, and safe-move behavior.
- Removed lingering `from src.` / `import src.` imports across runtime code,
  setup scripts, and tests.
- Improved keyboard responsiveness in `setup/hello_world.py`.
- Added gantry unlock support and related UI/backend plumbing for alarm
  recovery.
- Fixed a large PR review follow-up set around error propagation, health
  reporting, configuration validation, and GRBL driver guard rails.

### Test Result
- 578 tests passing except hardware-only tests that require a physical CNC.

## PR #15 (`uvvis-sql`) Rebase and Cleanup

### Work Done
- Rebased `uvvis-sql` onto current `main`.
- Resolved the `scan.py` merge conflict by keeping normalized UV-Vis
  measurement logging while preserving narrower exception handling.
- Removed line-ending noise from `src/instruments/uvvis_ccs/driver.py`.

### Result
- Final PR diff: 7 files changed, 288 insertions, 6 deletions.
- 524 tests passing with no regressions.

## DataReader and UV-Vis Analysis

### Work Done
- Added `data/data_reader.py` as a read-side query layer for campaigns,
  experiments, labware, and measurement tables.
- Added `data/analysis/uvvis.py` for UV-Vis record loading, spectrum unpacking,
  peak detection, absorbance calculation, and wavelength slicing.
- Added tests for the reader and the UV-Vis helpers.

### Test Result
- 558 tests passing with no regressions.

## Coordinate Convention Update

### Notes
- Updated the working-volume convention documentation around GRBL WPos.
- Updated gantry configs to reflect the intended operating coordinate ranges.
- Documented that older MPos-based workflow coordinates need recalibration
  before use in the newer convention.
