# 2026-04-28 — PR review fixes for phase-2 motion refactor

Branch: `codex/phase-2-refactor-motion`. Ran `/pr-review-toolkit:review-pr` (CRLF
filtered, configs/docs skipped) with code-reviewer, pr-test-analyzer, and
silent-failure-hunter agents in parallel. Acted on the 7 critical findings.

## Fixes applied

1. **#1 `src/protocol_engine/commands/home.py`** — already fixed by user before
   this session: the dual-branch `except ValueError: pass` that toggled between
   "preserve calibrated G54" and `zero_coordinates()` is gone. `home` now
   always preserves WCS. No further work needed.

2. **#2 `src/board/board.py:103-108`** (`Board.move_to_labware`) — removed the
   dead `del z` after `_validate_finite_xyz`. Validation now stands as an
   upstream sanity check on labware data without the misleading dead-code
   smell. Did **not** add `safe_approach_height >= labware.z` enforcement —
   existing tests document that approach < labware.z is legal because they
   live in different reference frames.

3. **#3 `src/deck/loader.py` `_resolve_user_z`** — investigated, then reverted.
   Existing tests (`tests/test_deck_loader.py:116, :143`) document that
   `total_z_height` is a deck-wide loader fallback intentionally ignored
   per-labware when `height` is supplied. The reviewer's "silent shift"
   concern doesn't match the design.

4. **#4 `setup/calibrate_deck_origin.py`** — replaced four broad
   `except Exception` blocks in `_recover_from_limit_alarm` and the inner jog
   loop with narrowed `(CommandExecutionError, StatusReturnError)` catches
   that re-raise instead of "continue". `MillConnectionError` always
   propagates. Imports `gantry.gantry_driver.exceptions`. Updated
   `_LimitRecoveringFakeGantry` to raise the right exception types and
   inverted `test_..._continues_when_recovery_readback_is_unavailable` to
   assert the new safe behavior (calibration aborts).

5. **#5 `tests/gantry/test_origin.py`** — new file, 26 tests covering all four
   public helpers in `src/gantry/origin.py`: `format_gcode_number` (negative
   zero, trailing-zero strip, rounding), `format_set_work_position_command`
   (per-axis, partial-axis, error path), `validate_deck_origin_minima` (zero,
   tolerance edge, each rejection branch), `build_deck_origin_calibration_plan`
   (ordering invariants on the GRBL safety sequence — `$10=0` before `G54`,
   `G92.1` before origin assignment, soft-limit disable before max-travel
   writes, re-home after re-enable).

6. **#6 `tests/validation/test_protocol_semantics.py`** — added 11 tests for
   the new working-volume bound checks. Exercises `_gantry_xyz_for_tip` with
   non-zero `offset_x` / `offset_y` / `depth` to catch sign or transposition
   errors that would otherwise only surface as a hardware crash. Covers
   inclusive boundary equality, travel_z violations independent of target,
   unknown named position, and the `gantry=None` backward-compat path.

7. **#7 `src/validation/protocol_semantics.py:262-268`** — narrowed bare
   `except Exception` in `_validate_move_waypoints` to
   `(KeyError, AttributeError, ValueError)` and now emits a
   `ProtocolSemanticViolation` ("position … cannot be resolved on the deck")
   instead of silently passing validation for typo'd labware names.

## Tests

`pytest tests/ --ignore=tests/hardware` → **1005 passed**, 2 unrelated
deprecation warnings (existing).

## Skipped reviewer findings

- **Reviewer #3** (deck loader silent `total_z_height` drop) — design intent;
  reverted my change after reading existing tests.
- **Move-to-labware approach-vs-labware-Z bounds check** — existing tests
  document this is allowed; no per-labware "top Z" available for a meaningful
  comparison.

## Files touched

- `src/board/board.py` — drop `del z`
- `src/validation/protocol_semantics.py` — narrow exception, emit violation
- `setup/calibrate_deck_origin.py` — narrow jog/recovery exceptions, import
  driver exception types
- `tests/gantry/test_origin.py` — new
- `tests/validation/test_protocol_semantics.py` — gantry-bound test cases
- `tests/setup/test_calibrate_deck_origin.py` — fake-gantry exception types,
  inverted readback-failure test
