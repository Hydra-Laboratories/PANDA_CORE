# 2026-02-10 - CNC Move Test Configuration

## Accomplished
- Modified `test_cnc_move.py` to accept command line arguments for axis, distance, feed rate, and serial port.
- Implemented a relative move sequence: +Distance, then -Distance (back to start).
- This allows testing movement on a new setup without modifying code, specifically for the requested 5mm out and back test.

## Notes
- To run the test: `python test_cnc_move.py --distance 5`
- Default arguments: Axis=X, Distance=5mm, Feed=500mm/min.
